# Per-Work Records

Create one folder per acquired work.

Each work folder should eventually contain:

- `work.md`
  - canonical bibliographic note
- `manifest.json`
  - machine-readable local paths, checksums, and processing state
- optional review notes for OCR, gaps, and editorial concerns
