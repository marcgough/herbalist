# Botanical Forward Shortlist

Date: 2026-06-21

Purpose:

Capture the next move after the mixed NLM lane added one thin `medical companion` witness plus two substantial practical-health books.

This note supersedes the older shortlist that centered the `1307`-work state after the household-practice and family-physician trio.

## What changed

- the archive now stands at `1310` chunked works
- `Corpus Memory` now holds `3300` retrieval documents
- `nlm-8206608`, `nlm-101306881`, and `nlm-2702091R` all landed cleanly and exact work-id retrieval was re-verified for each of them
- `nlm-8206608` surfaced in `thin-work-review` as `severe-thin-reference`
- `nlm-101306881` and `nlm-2702091R` stayed out of `thin-work-review`
- the batch therefore widened practical coverage, but it also showed that the selected NLM depth queue is not uniformly strong

## Current reserve state

The selected NLM reserve now stands at:

1. `nlm-101313340`
   - *Homoeopathic domestic physician*
   - Why still viable: it is the strongest remaining same-family practical witness and still lines up with `materia-medica`, `hygiene`, and remedies-oriented retrieval needs.
   - Why slower: it is still deliberate depth inside an already represented family.

2. `nlm-2561026R`
   - *The Edinburgh new dispensatory ...*
   - Why slower: repeat-heavy dispensatory depth rather than practical widening.

3. `nlm-64230310R`
   - *The sick man's friend*
   - Why slower: same-family botanical-principles depth after earlier related witnesses already landed.

4. `nlm-9717182`
   - *Pharmacopoeia*
   - Why slower: still too generic to outrank cleaner practical books.

5. `nlm-63570300R`
   - *The medical companion, or family physician*
   - Why now weaker: `nlm-8206608` from the same family landed as `severe-thin-reference`, which lowers confidence that another same-family witness deserves a near-term slot.

The selected Wellcome reserve still reads better as a deliberate later reference lane than as a blind immediate widening batch.

## Stronger next-step guidance

The safest next move should now be one of these:

1. manually screen one more uncovered NLM trio from the cleaner lower frontier
   - Why: the latest mixed batch improved coverage, but the thin result on `nlm-8206608` suggests uncovered breadth may again outperform blind depth.

2. tighten the selector against thin repeat-family witnesses before refreshing the reserve
   - Why: the current queue still over-values some reference-shaped or same-family depth holds.

3. if deliberate depth is still wanted immediately, take only `nlm-101313340`
   - Why: it is the clearest remaining same-family hold, while the rest of the reserve now looks slower, more generic, or newly weakened.

## Holds and cautions

- do not take the raw acquisition-frontier top ranks literally; they still over-admit Aristotle master-piece variants, sexual-physiology material, and other off-mission domestic-medicine noise
- keep `nlm-101125242` in the separate importer-recovery lane rather than letting it distort normal batch decisions
- keep the remaining selected Wellcome material as a deliberate later lane unless route quality improves or a specific reference purpose justifies it
- keep the `medical companion` family under lower confidence until another witness proves it can land with more than thin reference value
