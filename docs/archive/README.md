# Archived Notes

This folder keeps legacy working notes that should not drive the live Herbalisti corpus workflow anymore.

They are preserved for traceability, but the active corpus boundary is now defined by:

- `corpus-memory/`
- `corpus-memory/state.json`
- `scripts/corpus-memory/*`

Shared Agent Memory notes are not the source of truth for the Herbalisti corpus archive.
